{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "To-Do List Application - Assignment 1",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo Build step - no compilation required",
    "test": "jest --reporters=default --reporters=jest-junit",
    "test:local": "jest"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ]
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jest-junit": "^16.0.0",
    "supertest": "^6.3.4"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ipaddr.js": "^2.2.0"
  }
}
